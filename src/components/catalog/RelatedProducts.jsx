import { baseUrl } from '@/index';

export const RelatedProducts = () => {
  // fetch(`${baseUrl}/docs#p-category`)
  //   .then((res) => res.json())
  //   .then((json) => console.log(json));

  return <div>RELATED</div>;
};
